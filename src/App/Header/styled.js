import styled from "styled-components";

export const StyledHeader = styled.h1`
    padding: 20px 0;
    margin: 0;
    text-align: center;
    border-bottom: 1px solid ${({ theme }) => theme.color.tandora};
`;